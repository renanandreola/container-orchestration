import ReactDOM from 'react-dom';
import React from 'react';
import './index.css';
import App from './App';

const elementHTML = document.getElementById('root');

ReactDOM.render(
        <App/>, 
    elementHTML
);